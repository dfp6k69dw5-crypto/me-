services:
  - type: web
    name: alex-repo-simulator-mcp
    runtime: docker
    plan: free
    region: ohio
    dockerfilePath: ./Dockerfile.mcp
    dockerContext: .
    healthCheckPath: /mcp
    autoDeployTrigger: commit
