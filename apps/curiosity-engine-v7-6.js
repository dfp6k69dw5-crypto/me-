function init(){canvas=$('#canvas');ctx=canvas.getContext('2d');$('#addBtn').onclick=addInput;$('#wanderBtn').onclick=toggleWander;$('#compareBtn').onclick=comparePair;$('#weirdBtn').onclick=weirdest;$('#fitBtn').onclick=fitAll;$('#resetBtn').onclick=resetAll;$('#closeNode').onclick=()=>$('#nodeCard').classList.remove('open');$('#readerClose').onclick=closeReader;$('#setA').onclick=()=>{if(!selected)return;pairA=selected;updatePairbar();toast('Set as A')};$('#setB').onclick=()=>{if(!selected)return;pairB=selected;updatePairbar();toast('Set as B')};$('#transformBtn').onclick=()=>{if(selected){wanderStep++;transformNode(selected)}};$('#seedInput').addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key==='Enter')addInput()});canvas.addEventListener('pointerdown',onPointerDown);canvas.addEventListener('pointermove',onPointerMove);canvas.addEventListener('pointerup',onPointerUp);canvas.addEventListener('pointercancel',onPointerUp);canvas.addEventListener('wheel',onWheel,{passive:false});window.addEventListener('resize',setVisibleHeight);window.visualViewport?.addEventListener('resize',setVisibleHeight);window.visualViewport?.addEventListener('scroll',setVisibleHeight);setVisibleHeight();refresh();cancelAnimationFrame(raf);raf=requestAnimationFrame(loop)}
window.addEventListener('load',init);