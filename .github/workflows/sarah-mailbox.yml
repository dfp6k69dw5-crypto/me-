name: Sarah Conversation Room (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Public Sarah mailbox disabled. Private channel required."
