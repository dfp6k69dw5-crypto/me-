name: The Room Watchdog

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/room_reply_followup_once.py'

permissions:
  contents: write
  actions: write

jobs:
  watchdog:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 2

      - name: Apply live-event coherence repair
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python3 scripts/room_reply_followup_once.py

      - name: Restart The Room pulse only if it stopped
        if: github.event_name != 'push'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          active="$(gh run list \
            --repo "$GITHUB_REPOSITORY" \
            --workflow sarah-society-pulse.yml \
            --limit 20 \
            --json status \
            --jq '[.[] | select(.status == "in_progress" or .status == "queued" or .status == "pending" or .status == "waiting")] | length')"
          echo "Active Room pulse runs: $active"
          if [ "$active" = "0" ]; then
            gh workflow run sarah-society-pulse.yml --repo "$GITHUB_REPOSITORY" --ref main
          fi
